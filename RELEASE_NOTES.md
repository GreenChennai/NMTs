# NMTs 发布说明

## v3.0.2（拓扑编辑器架构重构：内置后端 + 实时同步）

- 架构重构：拓扑编辑器由「外置 pywebview 窗口 + CDN React Flow」改为 **NMTs 内嵌 HTTP + WebSocket 服务**（后端）+ **自包含纯 JS SVG 编辑器**（前端，零 CDN / 零 pywebview / 零 Node 构建）。
- 编辑器页面随二进制内嵌（`include_str!`），优先读取磁盘 `editor/topology_editor.html` 便于热改；彻底消除「需用户安装部署依赖」的问题，原 `[NMTs] 未安装 pywebview` 空色块问题消失。
- 实时双向同步：前端每次编辑（增删设备 / 拖拽 / 改端口 IP / 连线）经 `/ws` 实时回传 NMTs 后端，TUI 内存拓扑即时更新并重跑设计预检；`保存` 落盘 `topology.json`，`关闭` 关闭后端服务。
- 设备节点新增画布坐标 `x`/`y`（编辑器布局可持久化，后端逻辑忽略）。
- 移除 `editor/editor.py`（不再需要）。

## v3.0.1（拓扑编辑器修复）

- 修复：拓扑编辑器 `editor.py` 不再硬依赖 pywebview——未安装时自动回退系统默认浏览器打开编辑器；`topo_open_editor` 增加 python 缺失时的浏览器回退。
- 修复：二级菜单「打开编辑」改为**直接打开网页编辑器**（原误入设备列表）；新增「设备与 CLI（下发）」入口进入设备列表做 CLI 推导 / 串口下发。

## v3.0.0（UX 大版本重构）

V3.0 在 V2.0 基础上做交互范式层重构，7 项诉求按 M1–M7 里程碑落地，最终 `v3.0.0` 收口。

### 本版本新增

- **键位分层（NavController）**：顶层模块切换只走 `Tab` / `Ctrl+←` / `Ctrl+→`，普通 `←/→` 释放给各模块局部导航（子菜单 / 厂商切换 / 拓扑三级菜单）。
- **IME 守卫（ImeGuard）**：非输入态自动禁用输入法，单字母热键（R/F/T/G…）不再被组字窗拦截；`F2` 全局切换。
- **诊断分层 + 下钻（DrillPanel）**：全部体检 + 单项列表分隔展示；异常项独立「一键修复」，正常项 `Enter` 下钻看当前值并触发子动作（DNS 测速 / DNS 优选 / 路由追踪）。
- **DNS 双栈优选**：本机启用 IPv6 时测速含 v6 候选（`ping -6`），排名表带 `[v4]/[v6]` 标签，可单独/一并应用 v4+v6 DNS（`netsh interface ipv6 set dnsservers`）。
- **快捷设置统一面板（SettingsPanel）**：IPv4 静态/DHCP 单一开关 + 网关/掩码/IP/DNS 表单；IPv6 开关 + 同结构字段；MAC 只读；高级设置折叠区。
- **网工工具连后识别**：连前只选连接类型（通用/eNSP），连后自动 `display version`/`show version` 识别华为/H3C/Cisco 并自动切 CLI，识别失败可 `←/→` 手动覆盖。
- **拓扑多实例 + 三级导航**：多拓扑新建/导入/重命名/复制/导出/删除；`DeviceRole` 扩展 10 种（新增 Server/PC/Phone/AC/AP，`Unknown` 兼容旧 `.nmts`）；预检清单常驻旁栏。
- **外置编辑器富功能**：10 种设备图标、端口/接口 IP/掩码编辑、VLAN/路由设置、一键布局（水平/垂直/网格/间隔）、20px 网格吸附。

### 行为变更

- 普通 `←/→` 不再切换模块（改用 `Tab` / `Ctrl+←` / `Ctrl+→`）。
- DNS 优选默认协议改为 `both`（无 IPv6 时运行时自动回落 ipv4）。
- 快捷设置从「动作列表」改为「结构化面板」。

### 发布策略

- 本版本起 **仅推送源码**，GitHub Actions 改为 `workflow_dispatch` 手动触发：不再自动构建、不再自动发行 Release / 打 tag。

## v2.0.0（大版本收口）

V2.0 迭代收官：体验重构（v1.1~v1.6）+ 增强包（v1.7~v1.8）+ 大版本收口（v2.0）。

### 本版本新增

- **报告导出**：诊断结果一键导出 Markdown 报告（`G` 键，写入 `reports/diag_*.md`），含结论 + 检查结果表格，便于交接归档。
- **操作审计**：所有配置修改记录到 `logs/audit.log`（时间 / 动作 / 结果），满足运维合规。
- **跨模块联动收口**：诊断 → 自动修复（`F`）→ 备份 → 拓扑下发（`D`）→ 设备配置备份，五大模块贯通。
- **安装包**：`installer.nsi` 版本 2.0.0，CI 产出单文件 exe + 绿色版 zip + 安装包三件套。

### V2.0 全量回顾（v1.0 → v2.0）

| 版本 | 内容 |
| --- | --- |
| v1.1.0 | 统一 ScrollList + DNS 优选改确认 + 删冗余分组 |
| v1.2.0 | 手动静态 IP 表单 + IPv6 单一开关 |
| v1.3.0 | 网工工具连接状态机 + 持久会话实时回显 |
| v1.4.0 | 设备 running-config 备份归档 |
| v1.5.0 | 拓扑编辑器外置（React Flow + pywebview） |
| v1.6.0 | 统一组件收口（Toggle/StatusBadge + IPv6 状态可见） |
| v1.7.0 | 诊断实时化（路由追踪）+ 多网卡面板 |
| v1.8.0 | 脚本中心（通用排障命令）+ 分模块帮助引导 |
| v2.0.0 | 报告导出 + 操作审计 + 跨模块联动收口 |

### 已知限制（人工测试重点）

- 真机串口 / eNSP 连接、拓扑一键下发、设备 running-config 抓取需硬件验证。
- 拓扑编辑器依赖 `pip install pywebview` + CDN（React Flow），后续可离线内嵌。
- 静态 IP 表单需在真实网卡上验证；命令级超时（wait-timeout）未落地，靠子进程上限兜底。

## v1.8.0（V2.0 增强包二）

- **脚本 / 模板中心**：`vendor_db/common.yaml` 新增「通用排障命令」模板（ping / tracert / nslookup / ipconfig / arp / route / netstat / flushdns），网工工具厂商切换至「通用」即可免记命令执行。
- **帮助引导系统**：帮助面板重构为分模块按键速查，`F1` / `H` 唤起，降低上手成本。

> 配置漂移闭环（拓扑意图态 vs 设备 running-config 比对生成修复 CLI）依赖真机抓取，与跨模块联动一并收口至 v2.0.0。

## v1.7.0（V2.0 增强包一）

- **诊断实时化（路由追踪）**：模块一新增 `T` 键路由追踪（`tracert -d -h 8` 到公网 223.5.5.5），逐跳结果流式追加到实时日志，定位「断在哪一跳」。
- **多网卡可见**：诊断界面新增「网卡列表」面板，展示所有网卡及 Up/Down 状态、虚拟 / VPN 标记，多网卡场景一目了然。
- **执行反馈**：延续 `mpsc` 事件流模式（诊断 / DNS / 终端 / 探测各自 channel 流式回传），长任务不阻塞 UI。

## v1.6.0（V2.0 增量六：组件规范收口）

- **统一组件收口**：`ui/widgets.rs` 沉淀 `ScrollList` / `Toggle` / `StatusBadge`，header 管理员徽标统一为 `StatusBadge`，IPv6 开关以 `Toggle` 呈现当前状态（探测注册表 `DisabledComponents`）。
- **IPv6 状态可见**：探测合并查询返回 IPv6 启用状态，快捷设置列表实时显示「开/关」。
- **安装包**：`installer.nsi` 输出固定名 `NMTs-setup.exe`（版本号随 `Cargo.toml` 走），CI 打包 exe + 绿色版 zip + 安装包三件套。

## v1.5.0（V2.0 增量五：拓扑编辑器外置）

- **外置拓扑编辑器**：`editor/topology_editor.html`（React Flow 画布）+ `editor/editor.py`（pywebview 独立窗口）。主 TUI 点「拓扑图」→ 按 `O` 导出 `topology.json` 并拉起独立编辑器窗口。
- **编辑器能力**：设备类型按钮拖拽建节点（核心/汇聚/接入/路由/防火墙，带图标）、节点间连线、侧栏编辑设备属性（名称/主机名/厂商/角色）、保存 / 打开拓扑。
- **JSON 回读**：编辑器保存 `topology.json` 后，主程序按 `B` 回读并重新跑预检 + CLI 推导（保留 `topo_cli` 能力）。
- 新增 `topology.json` serde 往返单元测试，验证编辑器交换格式兼容。

> 依赖 `pip install pywebview`；网页资源暂用 CDN（React Flow），后续可内嵌离线化。

## v1.4.0（V2.0 增量四：设备配置备份）

- **备份已连接设备配置**：`backup::backup_device` 把已连接设备的 `running-config` 归档进 `.nmtsbak` 的 `devices/<device>_running.cfg`（manifest 标注 `kind: device` + 厂商）。
- **模块五新增动作**：「备份已连接设备配置」——仅在模块三已连接时可用，否则提示先连接；发送 `display current-configuration`（VRP）/ `show running-config`（IOS）抓取归档。

## v1.3.0（V2.0 增量三：网工工具连接状态机）

- **连接状态机**（未连接 → 扫描 → 已连接）：未连接时仅显示串口列表 + 扫描入口（管理命令不暴露）；扫描成功后建立**持久串口会话**，后台读线程持续回显。
- **实时终端回显**：已连接态显示终端输出流 + 手动输入模式（`I` 进入、回车发送、`Esc` 退出）。
- **已连接才显管理**：连接成功后命令模板才可选中发送；断线自动回退未连接态。

## v1.2.0（V2.0 增量二：表单与开关）

- **手动静态 IP 表单**：`net_set::set_static_ip` 落地，模块二新增「设置静态 IP」表单（IP/掩码/网关/主备 DNS 五项，↑/↓ 移焦点、字符输入、Enter 应用），应用前自动备份。
- **IPv6 单一开关**：原「开启 IPv6 / 关闭 IPv6」两个对立项合并为「IPv6 开关」单项（读取注册表 `DisabledComponents` 判断当前状态后反向切换）。

## v1.1.0（V2.0 增量一：体验重构）

- **统一 ScrollList 组件**（`ui/widgets.rs`）：`List` + 右侧 `Scrollbar` + 自动滚动保证选中项可见，替换五大模块原来的 `Paragraph` 整段直渲，根治「列表超屏不翻页」。
- **DNS 优选改为「先优选、再确认」**：测速仅产出排名表（延迟 / 国家 / 是否可达），**不再静默写入网卡**；用户在排名表 ↑/↓ 选中、Enter 确认后才应用，应用前自动备份原 DNS 可回退。
- **消除冗余**：删除模块二「备份/恢复」项（统一走模块五）与「设置 DNS：阿里/腾讯/Google/Cloudflare」4 个固定单项（并入 DNS 优选排名表）；操作按 DNS / IP / 协议 / 优化语义分组。

## v1.0.0

首个完整版本：五大模块全部落地 + 模块联动 + 安装包。

### 新增

- **诊断→修复闭环**：诊断结果中的「自动修复」项一键执行（按 `F`），逐条回显执行命令与结果。
- **拓扑一键下发**：模块四生成的 CLI 可一键下发到串口设备（按 `D`），打通「拓扑 → CLI → 下发」链路。
- **安装包**：`installer.nsi`（NSIS）生成安装包，CI 同时产出绿色版 zip + 单文件 exe + 安装包，`v*` tag 自动发布 GitHub Release。

### 五大模块回顾（v0.1 ~ v0.6）

1. 网络诊断：三层诊断模型（基础 / 本机环境 / 外部因素），14 项检查 + 一键修复 + 指导兜底。
2. 快捷设置：DNS / IP / DHCP / IPv6 / TCP 优化 + 备份回退 + DNS 优选引擎。
3. 网工工具：串口枚举 + 厂商命令模板（华为 / H3C / Cisco）。
4. 拓扑图：建模 + 前置条件预检 + CLI 推导 + D2 导出。
5. 配置备份：`.nmtsbak` zip 归档备份 / 恢复。

### 已知限制（人工接入测试重点）

- 真实串口连接 / 波特率试探、拓扑一键下发需硬件（路由器 / 交换机 Console 口）验证。
- 拓扑画布为内置演示拓扑，拖拽编辑待后续版本。
- 静态 IP / 掩码 / 网关的手工录入、`apply_when_better_than_current` 细节待完善。

## v0.6.0

第六个里程碑：模块二 DNS 优选引擎。

### 新增

- **DNS 优选引擎**（`core/dns.rs`）：加载内嵌 `dns_providers.yaml`（74 条），按「类别（默认/家庭/安全）× 协议（IPv4/IPv6）」筛选候选，并发 ping 测速（RTT），`prefer_country` 命中者优先，产出排名表，一键应用最优（主 + 备 DNS）。
- **快捷设置集成**：模块二新增「DNS 优选（测速+应用最优）」，测速过程异步执行、实时显示排名（名称/国家/延迟），自动写入当前网卡 DNS。
- RTT 解析兼容中英文 ping 输出（`时间=8ms` / `time=8ms` / `时间<1ms`）。

### 已知限制

- 测速以 ping RTT 为就近代理，个别禁 ping 的 DNS 服务器可能被判「超时」（不影响其它候选）。
- `apply_when_better_than_current`（仅优于当前才应用）未完全落地，当前直接应用最优可达候选。

## v0.5.0

第五个里程碑：模块四「拓扑图工具」（设计 + 预检 + CLI 推导）。

### 新增

- **拓扑数据模型**（`core/topology.rs`）：`Device` / `DeviceConfig` / `Vlan` / `L3Intf` / `PortConf` / `Link` / `Credentials` 完整参数模型，可 serde 序列化存档；`petgraph` 无向图构建 + 邻居查询。
- **前置条件预检**（`core/design_check.rs`）：声明式 `Intent` 约束求值，输出问题清单（Error/Warn/Info）——子网重叠、VLAN 未放行、冗余上联、STP 环路、路由协议不一致、MTU 不一致、单点故障。
- **CLI 推导**（`core/topo_cli.rs`）：据设备参数 + 厂商（华为/H3C VRP、Cisco IOS）生成主机名 / VLAN / trunk / access / 三层接口 / 静态路由配置。
- **D2 导出**（`export_d2`）：生成 `.d2` 文本，`E` 键导出并尝试调用 `d2` CLI 渲染 SVG。
- **拓扑界面**（模块四）：内置演示拓扑，设备列表 / 链路、预检问题清单（severity 着色）、CLI 预览。

### 已知限制

- 拓扑画布编辑（拖拽画图）规划于后续版本，当前为「模型 + 预检 + 推导」核心，内置演示拓扑。
- 设备参数编辑窗口、与模块三的一键下发联动（自动连接 / 配置漂移）规划于 v1.0。

## v0.4.0

第四个里程碑：模块五「配置备份 / 恢复」。

### 新增

- **本机网络配置备份**（`core/backup.rs`）：采集 `netsh interface ip/ipv6 dump` + 无线 profile 导出 + 网络相关注册表导出，写入 `manifest.json` 元信息，打包为 `.nmtsbak`（zip 归档）。
- **一键恢复**：解压 `.nmtsbak`，`netsh -f` 回放 IP 配置、`netsh wlan add profile` 重新导入无线配置、`reg import` 导入注册表。
- **备份界面**（模块五）：备份 / 恢复最近备份 / 刷新备份列表，展示已备份的 `.nmtsbak` 清单。
- zip 打包 / 解压逻辑含往返单元测试（跨目录、二进制安全）。

### 已知限制

- 设备（路由器 / 交换机）运行配置备份需经模块三抓取（`show run` / `dis cu`），规划于 v1.0 联动落地。
- 恢复后需重启网卡 / 重连无线以完全生效。

## v0.3.0

第三个里程碑：模块三「网工工具（终端）」。

### 新增

- **厂商命令模板库**（`core/vendor_cli.rs`）：加载内嵌 `vendor_db/*.yaml`（华为 / H3C / Cisco 三厂商），命令模板按「分类 → 功能 → 命令 + 参数」组织，UI 免记命令。
- **串口封装**（`core/serial.rs`）：枚举可用 COM 口（`serialport`）、打开会话、写命令、波特率试探（按顺序发 `\r` 等回显命中）。
- **网工终端界面**（模块三）：厂商切换（`[`/`]`）、命令模板列表（↑/↓）、命令详情 / 参数、串口列表；`Enter` 发送当前命令到串口（无串口时展示命令文本供手动复制）。
- 兼容 eNSP（本地串口管道 / telnet，实际连接经串口统一入口）。

### 已知限制

- 真实串口连接 / 波特率试探需硬件环境（路由器 / 交换机 Console 口）人工验证；沙箱仅覆盖枚举、试探判定、命令渲染的单元测试。
- 交互式终端（实时回显滚动 + 输入缓冲）规划于后续版本完善。

## v0.2.0

第二个里程碑：启动 / 诊断提速 + 模块一三层诊断补全 + 模块二快捷设置。

### 新增 / 改进

- **启动提速**：环境探测改为后台异步（`spawn_blocking` + mpsc 回传），UI 秒开，不再启动阻塞 3~4 秒。
- **诊断提速**：新增 `windows/probe.rs` 单次 PowerShell 合并查询，把原 7 次 PowerShell 冷启动合并为 1 次，DNS 解析也合并进探测；ping 网关 / ping 公网并发执行、单包探测。`diag --quick` 全链路从 ~20s 降至 ~4s。
- **模块一补全三层**（共 14 项检查）：
  - 基础层：当前上网网卡 / DHCP·IP / 默认路由 / 网关连通 / DNS 解析 / 系统代理 / 虚拟网卡干扰
  - 本机环境层：网卡驱动状态 / 物理链路状态 / MTU
  - 外部因素层：外网连通（路由器/光猫）/ 病毒威胁 / 二层环路风险 / MAC 锁排查
- **模块二快捷设置**（13 项操作）：刷新 DNS、设置 DNS（阿里/腾讯/Google/Cloudflare）、DNS/IP 切回 DHCP、释放续租 IP、开关 IPv6、TCP 全局优化、备份 / 恢复网络配置。修改前 `netsh dump` 备份到 `backups/`，可一键回退。

### 已知限制

- 静态 IP / 掩码 / 网关的手工录入、DNS 优选引擎（v0.6）尚未落地。
- 命令级超时（wait-timeout）待后续版本；当前靠 ping 单包 1s 超时 + 子进程上限。
- 模块三（网工终端）、四（拓扑）、五（备份）仍为占位，规划于 v0.3~v0.5。

## v0.1.0（草稿）

首个里程碑：项目骨架 + TUI 框架 + 配置加载 + 模块一「网络诊断」基础层。

### 新增

- **项目骨架**：Rust (stable, MSVC) + `ratatui` / `crossterm` TUI，`build.rs` 嵌入 UAC 清单（`requireAdministrator`）。
- **配置体系**：`config/default.yaml` 默认配置内嵌二进制，运行时 `config/config.yaml` 覆盖，`serde_yaml` 加载 / 保存。
- **数据文件**：
  - `vendor_db/dns_providers.yaml` — DNS 优选数据源（74 条，含 CN 国内节点补齐）。
  - `vendor_db/{huawei_vrp,h3c_vrp,cisco_ios}.yaml` — 三厂商命令模板（模块三/四备用）。
- **windows 封装层**：子进程静默执行（GBK/UTF-8 解码、超时）、管理员检测、netsh / PowerShell 封装、多网卡枚举与「当前上网网卡」判定（默认路由 + 跃点排序 + 虚拟网卡过滤）。
- **模块一（基础层诊断）**：当前上网网卡判定、DHCP / IP 配置、默认路由 / 网关、网关连通、DNS 解析、系统代理检测、虚拟网卡干扰，共 7 项检查，逐项区分 `正常 / 警告 / 异常` 与 `自动修复 / 手动步骤`。
- **执行反馈**：诊断过程经 `mpsc` 流式推送进度 / 结果 / 命令回显，TUI 步骤清单 + 实时日志面板实时刷新，杜绝「看着像卡死」。
- **CLI 模式**：`nmts diag --quick` 无界面快速诊断，可脚本化。

### 已知限制

- 模块二～五为占位界面，规划于 v0.2～v0.6 逐步落地。
- 诊断本机环境层（驱动 / MTU）与外部因素层（病毒 / 环路 / MAC 锁）规划于 v0.2。
- `Get-NetAdapter` / `Get-NetIPConfiguration` 依赖 Windows 8+（目标平台 Win10/11）。
